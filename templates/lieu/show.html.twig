{% extends 'base.html.twig' %}

{% block title %}Lieu{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row d-flex justify-content-center">
        <div class="col-10 col-sm-4">
            <h1>Lieu</h1>

            <table class="table">
                <tbody>
                    <tr>
                        <th>Nom</th>
                        <td>{{ lieu.nom }}</td>
                    </tr>
                    <tr>
                        <th>Rue</th>
                        <td>{{ lieu.rue }}</td>
                    </tr>
                    <tr>
                        <th>Latitude</th>
                        <td>{{ lieu.latitude }}</td>
                    </tr>
                    <tr>
                        <th>Longitude</th>
                        <td>{{ lieu.longitude }}</td>
                    </tr>
                </tbody>
            </table>


            <a class="btn btn-primary mb-2" href="{{ path('lieu_edit', {'id': lieu.id}) }}">Modifier</a>

            {{ include('lieu/_delete_form.html.twig') }}

            <a href="{{ path('lieu_index') }}">retour à la liste</a>
        </div>
    </div>
</div>
{% endblock %}