{% extends 'base.html.twig' %}

{% block title %}Campus{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row d-flex justify-content-center">
        <div class="col-10 col-sm-4">
            <h1>Campus</h1>

            <table class="table">
                <tbody>
                    <tr>
                        <th>Nom</th>
                        <td>{{ campus.nom }}</td>
                    </tr>
                </tbody>
            </table>

            <a class="btn btn-primary mb-2" href="{{ path('campus_edit', {'id': campus.id}) }}">Modifier</a>

            {{ include('campus/_delete_form.html.twig') }}

            <a href="{{ path('campus_index') }}">retour à la liste</a>
        </div>
    </div>
</div>
{% endblock %}