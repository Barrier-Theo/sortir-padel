{% extends 'base.html.twig' %}

{% block title %}Ajout état{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row d-flex justify-content-center">
        <div class="col-4">
            <h1>Ajouter un état</h1>

            {{ include('etat/_form.html.twig') }}

            <a href="{{ path('etat_index') }}">retour à la liste</a>
        </div>
    </div>
</div>
{% endblock %}